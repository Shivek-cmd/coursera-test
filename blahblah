#include<iostream>
#include<string>

using namespace std;
void check( string a)
{
	int i, count = 1;
	for (i = 0; a[i] != '\0'; i++)
	{
		
	}
	cout << count;
}
int main()
{
	string s;
	getline(cin, s);
	check(s);


}
